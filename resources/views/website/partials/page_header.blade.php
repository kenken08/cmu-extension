<div class="row mt-5s">
    <div class="col-sm-{{ isset($column) ? $column : '8' }}">
        <h1 class="page-header text-dark">
            {!! isset($title) ? $title : $title !!}
        </h1>
    </div>
</div>
